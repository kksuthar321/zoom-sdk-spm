import XCTest
@testable import Zoom_SPM

final class Zoom_SPMTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Zoom_SPM().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
